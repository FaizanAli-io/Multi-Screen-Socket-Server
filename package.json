{
  "version": "1.0.0",
  "main": "server.js",
  "name": "multi-screen-socket-server",
  "description": "Node socket server for synchronized playback in Multi Screen Fitness Displays",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
